using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CodingChallenges.HackerRank.WarmUp
{
    /// <summary>
    /// You are given an integer N. Find the digits in this number that exactly divide N (division that leaves 0 as remainder) and display their count. 
    /// For N=24, there are 2 digits (2 & 4). Both of these digits exactly divide 24. So our answer is 2.
    /// </summary>
    class FirstDigit
    {
        //static void Main(String[] args)
        //{
        //    int cases = Convert.ToInt32(Console.ReadLine());
        //    for (int i = 0; i < cases; i++)
        //    {
        //        int count = 0;
        //        long ival = Convert.ToUInt32(Console.ReadLine());
        //        long m = ival;
        //        while (m > 0)
        //        {
        //            long curDigit = m%10;
        //            m /= 10;
        //            if (curDigit != 0 && ival%curDigit == 0)
        //            {
        //                count ++;
        //            }

        //        }
        //        Console.WriteLine(count);
        //    }
        //}
    }
}
